<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sky.mapper.DishMapper">
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT into dish(name,category_id,price,image,description,status,create_time,update_time,create_user,update_user)
        VALUES
        (#{name},#{categoryId},#{price},#{image},#{description},#{status},#{createTime},#{updateTime},#{createUser},#{updateUser})
    </insert>
    <update id="update">
        UPDATE dish
        <set>
            <if test="name != null">
                name = #{name},
            </if>
            <if test="categoryId != null">
                category_id = #{categoryId},
            </if>
            <if test="price != null">
                price = #{price},
            </if>
            <if test="image != null">
                image = #{image},
            </if>
            <if test="description != null">
                description = #{description},
            </if>
        </set>
        WHERE id = #{id}
    </update>
    <select id="pageQuery" resultType="com.sky.entity.Dish">
        SELECT *  FROM dish
        <if test="name != null and name != ''">
            WHERE name LIKE concat('%',#{name},'%')
        </if>
    </select>
    <select id="getByIdWithFlavor" resultType="com.sky.vo.DishVO">
        SELECT d.id, d.name, d.category_id, d.price, d.image, d.description,df.name, df.value
        FROM dish d
                 LEFT JOIN dish_flavor df ON d.id = df.dish_id
        WHERE d.id = #{id}
    </select>
</mapper>